<div id="pet-<%=pet.id%>" class="carousel slide" data-bs-ride="true">
  <div class="carousel-indicators">
    <%  pet.photos.each_with_index do |photo, index| %>
       <button type="button"  data-bs-target="#pet-<%=pet.id%>" data-bs-slide-to="<%=index%>" class="<%= index == 0 ? 'active' : '' %>"aria-current="true" aria-label="Slide <%=index%>"></button>
    <% end %>
  </div>
  <div class="carousel-inner">
    <%  pet.photos.each_with_index do |photo, index| %>
      <div class="carousel-item <%= index == 0 ? 'active' : '' %>">
          <%= cl_image_tag photo.key, class:"d-block rounded-top  object-fit-cover", style: "width:100%; aspect-ratio: 16 / 9;", crop: :fill%>
      </div>

    <% end %>
  </div>
  <button class="carousel-control-prev" type="button" data-bs-target="#pet-<%=pet.id%>" data-bs-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Previous</span>
  </button>
  <button class="carousel-control-next" type="button" data-bs-target="#pet-<%=pet.id%>" data-bs-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="visually-hidden">Next</span>
  </button>
</div>
